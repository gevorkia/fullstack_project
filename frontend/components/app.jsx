import React from "react";
import { Route } from "react-router-dom";

const App = () => (
  <div>
    <header>
      <h1>We are in App.jsx!</h1>
      {/* container for auth */}
    </header>
  </div>
);

export default App;